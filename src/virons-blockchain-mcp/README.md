# Virons blockchain MCP Server

Port: 9430

## Tools

- `example_tool` - Example tool (replace with actual tools)

## Usage

```bash
# Run locally
python server.py

# Build Docker image
docker build -t virons-blockchain-mcp .

# Run container
docker run -p 9430:9430 virons-blockchain-mcp
```
