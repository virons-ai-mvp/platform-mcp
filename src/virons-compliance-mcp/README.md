# Virons compliance MCP Server

Port: 9560

## Tools

- `example_tool` - Example tool (replace with actual tools)

## Usage

```bash
# Run locally
python server.py

# Build Docker image
docker build -t virons-compliance-mcp .

# Run container
docker run -p 9560:9560 virons-compliance-mcp
```
