# Virons security MCP Server

Port: 9570

## Tools

- `example_tool` - Example tool (replace with actual tools)

## Usage

```bash
# Run locally
python server.py

# Build Docker image
docker build -t virons-security-mcp .

# Run container
docker run -p 9570:9570 virons-security-mcp
```
