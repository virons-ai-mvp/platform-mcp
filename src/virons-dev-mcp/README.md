# Virons dev MCP Server

Port: 9500

## Tools

- `example_tool` - Example tool (replace with actual tools)

## Usage

```bash
# Run locally
python server.py

# Build Docker image
docker build -t virons-dev-mcp .

# Run container
docker run -p 9500:9500 virons-dev-mcp
```
