# Virons prompts MCP Server

Port: 9550

## Tools

- `example_tool` - Example tool (replace with actual tools)

## Usage

```bash
# Run locally
python server.py

# Build Docker image
docker build -t virons-prompts-mcp .

# Run container
docker run -p 9550:9550 virons-prompts-mcp
```
