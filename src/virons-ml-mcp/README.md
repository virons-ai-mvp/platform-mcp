# Virons ml MCP Server

Port: 9420

## Tools

- `example_tool` - Example tool (replace with actual tools)

## Usage

```bash
# Run locally
python server.py

# Build Docker image
docker build -t virons-ml-mcp .

# Run container
docker run -p 9420:9420 virons-ml-mcp
```
